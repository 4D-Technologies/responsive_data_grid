name: responsive_data_grid
description: Responsive Data Grid for Flutter inspired by Kendo UI's Grid functionality with full filtering, sorting, and paging that also responds dynamically to screen size using the same metaphor as bootstrap grid.
version: 0.0.13
homepage: https://github.com/Concerti-IO/responsive_data_grid/tree/main/responsive_data_grid
repository: https://github.com/Concerti-IO/responsive_data_grid

environment:
  sdk: ">=2.14.0 <3.0.0"
  flutter: ">=2.2.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0

  bootstrap_grid: ^1.0.1
  client_filtering: ^0.0.15
  date_time_picker: ^2.1.0
  rxdart: ^0.27.2
  infinite_scroll_pagination: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
